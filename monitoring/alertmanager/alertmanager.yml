route:
  receiver: "default"
  group_by: ["alertname", "job"]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 4h

receivers:
  - name: "default"
    # No external integrations configured by default.
    # Add Slack/Email/PagerDuty here when needed.
